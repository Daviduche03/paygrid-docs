# API reference

Every endpoint in the public API, with parameters, request bodies, responses,
and error handling.

> **Generated by Weldrr.** The documentation agent replaces this placeholder
> with a reference page per resource/endpoint, driven by `docs.md` and
> `spec.json`. Create one page per resource (e.g. `customers.mdx`,
> `invoices.mdx`) — they are discovered automatically and appear in the sidebar.
